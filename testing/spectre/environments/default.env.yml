rabbitmq:
  sample:
    host: localhost
    port: 5672
    username: developer
    password: dev
    virtual_host: /
    queue:
      name: sample_q
    exchange:
      name: sample_x
      durable: true
    routing_key: some_routing_key
